const log = (x: number, base: number) => Math.log(x) / Math.log(base);
const log2 = (x: number) => Math.log2(x);
const log10 = (x: number) => Math.log10(x);
const ln = (x: number) => Math.log(x);

export { log, log2, log10, ln };
